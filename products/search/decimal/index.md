# decimal


The `decimal` data type represents a 64-bit wide, double-precision, floating-point number.

Aliases: [`double`](double), [`real`](real).

- `decimal("123.4")` returns `123.4`
- `decimal("1e3")` returns `1000`
- `decimal("hello")` returns `null`
