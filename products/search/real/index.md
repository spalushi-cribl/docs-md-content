# real


The `real` data type represents a 64-bit wide, double-precision, floating-point number.

Aliases: [`decimal`](decimal), [`double`](double).

- `real("123.4")` returns `123.4`
- `real("1e3")` returns `1000`
- `real("hello")` returns `null`
