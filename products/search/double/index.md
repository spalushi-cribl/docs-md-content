# double


The `double` data type represents a 64-bit wide, double-precision, floating-point number.

Aliases: [`decimal`](decimal), [`real`](real).

- `double("123.4")` returns `123.4`
- `double("1e3")` returns `1000`
- `double("hello")` returns `null`
